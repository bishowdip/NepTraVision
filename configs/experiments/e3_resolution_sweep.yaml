# E3 — Resolution sweep: accuracy <-> speed trade-off; feeds the plate question.
# Output: results/tables/table3_resolution.csv.
id: e3_resolution_sweep
description: Best small model across input resolutions.
models: [yolov8n]            # set to the best small model from E1/E2
seeds: [0, 1, 2]
imgsz_sweep: [320, 416, 640]
hardware_tiers: [cpu_laptop, rpi5]
metrics: [mAP50, fps]
